import React from 'react';
import listenImage from '/listen.jpg'; // Adjust the path as needed

const ListenAnywhere = () => (
  <section className="listen-anywhere" style={{ backgroundImage: `url(${listenImage})` }}>
    <div className="devices">
    </div>
  </section>
);

export default ListenAnywhere;
